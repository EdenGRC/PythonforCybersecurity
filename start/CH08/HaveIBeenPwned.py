#!/usr/bin/env python3
# Script that checks passwords agains haveibeenpwned.com API
# https://haveibeenpwned.com/API/v3#PwnedPasswords
# By 

import requests

#Define my API key????
API_KEY = "xxxxxxxxxxxxxxxxxxxxxxxxxxx "

# URL we are querying for endpoint of ip lookup service
Base URL = "https://mqw.virustota1.com/vtapi/v2"

def check
Check if an IP address is good or bad utilzing virus total
url = f" {Base_URL}/ip-address/report"
params = {
"apikey" : API KEY,
. ip_address
"ip"
requests.get(url, params=params)
response =
